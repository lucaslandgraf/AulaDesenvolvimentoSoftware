
public interface Animal { // Não usa o termo class
	
	/* Todas as classes que IMPLEMENTS esta interface devem escrever (implementar) os métodos definidas abaixo */
	void som();
	void mover();
}
